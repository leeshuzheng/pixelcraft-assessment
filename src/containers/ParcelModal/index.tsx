import React from "react";
import { ParcelModalProps } from "./definitions";

const ParcelModal: React.FC<ParcelModalProps> = () => {
  return <div className="absolute max-w-[1200px] w-full"></div>;
};

export default ParcelModal;
